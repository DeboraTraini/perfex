workflows:
  ios-release-build:
    name: iOS Release Build
    environment:
      vars:
        APP_ID: "com.perfex.admin"
        BUNDLE_ID: "com.perfex.admin"
        CERTIFICATE_PASSWORD: "debbere"
        PROVISIONING_PROFILE_NAME: "Horizone_AdHoc_Profile"
        CERTIFICATE_NAME: "ios"
      ios_signing:
        certificates:
          - certificate_name: ios
            password: debbere
        provisioning_profiles:
          - name: Horizone_AdHoc_Profile
            bundle_id: com.perfex.admin
    scripts:
      - name: Install dependencies
        script: |
          flutter pub get
      - name: Build iOS app
        script: |
          flutter build ipa --release --export-options-plist=ExportOptions.plist
    artifacts:
      - build/ios/ipa/*.ipa